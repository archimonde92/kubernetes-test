image:
  repository: archimonde13/color-server
  tag: 1.1.5

service:
  type: NodePort
  ports:
    colorServer: ${PORT}
  nodePort: 30001
resources:
  limits:
    memory: 1Gi

secret:
  name: color-server-secret

envSecret:
  PORT: ${PORT}
  COLOR: ${COLOR}